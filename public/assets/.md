You are a Senior Frontend Engineer working on an Astro + TailwindCSS project.

Goal: Audit and fix all text alignment across this page so that every section looks visually and logically centered to a normal human viewer — not just technically centered.

Guidelines:
1. Use Tailwind’s utilities for centering:
   - Horizontal: `text-center`, `mx-auto`, `justify-center`
   - Vertical: `items-center`, `place-items-center`, `flex-col`, `min-h-[X]`
2. Group related items (icons + headings + paragraphs) inside a parent flex or grid container and center **the group**, not each element independently.
3. Preserve the existing hierarchy and spacing rhythm — use padding, `gap-y-*`, and `leading-relaxed` to maintain balance.
4. Keep text width readable: add `max-w-prose` or `max-w-[60ch]` with `mx-auto` when needed.
5. Verify responsiveness:
   - Mobile: text remains centered and readable (no overflow or cramped edges)
   - Desktop: alignment looks naturally balanced inside section width or grid
6. Don’t flatten layout structure — fix alignment using Tailwind utilities or minimal wrapper divs only.
7. Add comments explaining logic changes (e.g., “Centered group horizontally using flex container for icon + text”).

Definition of Done:
- All text feels visually centered inside its logical container (hero sections, cards, modals, etc.).
- Alignment consistent on mobile and desktop breakpoints.
- No uneven whitespace or double centering.
- Visual hierarchy remains intact.
